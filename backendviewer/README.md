# AR Care System — Architecture Flowchart

Open `flowchart.html` in a browser. See the [main README](../README.md#system-architecture) for details and keyboard shortcuts.

## Recording

```bash
pip install playwright pillow numpy
playwright install chromium
python record_gif.py
```
